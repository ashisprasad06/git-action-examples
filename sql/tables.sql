create table ${database_name}.${table_name} (
    col_a integer,
    col_b integer
);

create table ${database_name}.${table_name} (
    col_c integer,
    col_d integer
);

create table ${database_name}.${table_name} (
    col_e integer,
    col_f integer
);
